apiVersion: v1
kind: Service
metadata:
  name: llm-explorer
  namespace: llm-explorer
  labels:
    app: llm-explorer
spec:
  type: ClusterIP
  selector:
    app: llm-explorer
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
